{
  "name": "qa-lab-bank",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.cjs",
    "demo": "playwright test tests/e2e/US-*.spec.ts --headed",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "report": "playwright show-report",
    "generate": "node generator/generate.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "yaml": "^2.8.1"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
